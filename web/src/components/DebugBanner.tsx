export default function DebugBanner() {
  return (
    <div style={{ background: "#ffedd5", border: "1px dashed #ea580c", padding: "8px", textAlign: "center" }}>
      Debug: Layout is active and styles are loading.
    </div>
  );
}
