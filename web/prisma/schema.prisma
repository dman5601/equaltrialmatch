datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id           String       @id @default(cuid())
  email        String       @unique
  password     String
  savedTrials  SavedTrial[]
  profile      Profile?     // ← implicit one-to-one
}

model Profile {
  id        String   @id @default(cuid())
  userId    String   @unique
  user      User     @relation(fields: [userId], references: [id])
  zip       String?
  radius    Int?
  age       Int?
  gender    String?
  phase     String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model SavedTrial {
  id        String   @id @default(cuid())
  user      User     @relation(fields: [userId], references: [id])
  userId    String
  nctId     String
  createdAt DateTime @default(now())

  @@unique([userId, nctId])
}
